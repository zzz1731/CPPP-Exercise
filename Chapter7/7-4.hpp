#pragma onece
#ifndef _Person
#define _Person

#include <string>
using std::string;

class Person
{
private:
    string strName;
    string strAddress;
};

#endif

//编写一个名为 Person 的类，使其表示人员的姓名和住址。使用string对象存放这些元素，接下来的练习将不断充实这个类的其他特征。
//Wed,Aug28,2019