# 7-8

## 为什么 _read_ 函数将其 _Sales_data_ 参数定义成普通的引用，而 _print_ 将其参数定义成常量引用？

## _read_ 函数将其 _Sales_data_ 参数定义成普通的引用是因为我们需要从标准输入流中读取数据并将其写入到给定的 _Sales_data 对象，因此需要有修改对象的权限。而 _print_ 将其参数定义成常量引用是因为它只负责数据的输出，不对其做任何的更改。

&nbsp;

Wed,Aug28,2019